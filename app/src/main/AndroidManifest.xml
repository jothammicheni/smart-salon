<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/rabitlogo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/rabitlogo"
        android:supportsRtl="true"
        android:theme="@style/Theme.WitxSalon"
        tools:targetApi="31">
        <activity
            android:name=".Chats.ChatActivity"
            android:exported="false" />
        <activity
            android:name=".RegisterVet"
            android:exported="false" />
        <activity
            android:name=".DisplayItemObjects"
            android:exported="false"
            android:label="@string/title_activity_display_item_objects"
            android:theme="@style/Theme.WitxSalon" />
        <activity
            android:name=".adminTasks.AdminPanel"
            android:exported="true"
            android:label="@string/title_activity_admin_panel"
            android:theme="@style/Theme.WitxSalon" />
        <activity
            android:name=".adminTasks.AddNewProduct"
            android:exported="false" />
        <activity
            android:name=".AdminAuth.AdminLogin"
            android:exported="false"
            android:label="@string/title_activity_admin_login"
            android:theme="@style/Theme.WitxSalon" />
        <activity
            android:name=".Register"
            android:exported="false"
            android:label="@string/title_activity_register"
            android:theme="@style/Theme.WitxSalon" /> <!-- <activity -->
        <!-- android:name=".ui.login.Register" -->
        <!-- android:exported="false" -->
        <!-- android:label="@string/title_activity_register" /> -->
        <activity
            android:name=".login"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- <activity -->
        <!-- android:name=".ui.login.login" -->
        <!-- android:exported="false" -->
        <!-- android:label="@string/title_activity_login" /> -->
        <activity
            android:name=".SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.WitxSalon.NoActionBar" />
    </application>

</manifest>